scenario_id: scn.data-exposure.prompt-egress
statement: >
  Customer data classes not authorised for external inference leave the processing boundary in a prompt to a third-party model endpoint.
asset: "customer data in scope of the DPA"
threat_actor: "internal actor without malicious intent"
effect: confidentiality
estimation_level: lef
estimation_rationale: >
  Most likely realised as an error scenario rather than a malicious one. Frequency should be measured from egress telemetry once ctl.ai.inference.model-inventory is operating; until then it is not usefully estimable.
parameters:
  loss_event_frequency:
    min: 0.0
    most_likely: 0.0
    max: 0.0
    unit: events_per_year
    provenance:
      derivation_level: assumed
      source: "UNCALIBRATED — populate at the Day-45 calibration workshop. See _CALIBRATION-STATUS.md."
      confidence_tier: 1
  primary_loss_magnitude:
    min: 0.0
    most_likely: 0.0
    max: 0.0
    unit: usd
    provenance:
      derivation_level: assumed
      source: "UNCALIBRATED — FAIR-MAM module decomposition required. See _CALIBRATION-STATUS.md."
      confidence_tier: 1
controls: [ctl.ai.inference.model-inventory]
