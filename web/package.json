{
  "name": "grc-program-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosted demo GUI for grc-program. Runs the real tool out-of-process against the bundled synthetic fixtures and renders the control inventory (the system of record), the DuckDB evidence pipeline (landing -> assertions -> variance), control health and the four-direction gap assessment, and the emitted OSCAL package. Never contacts a real system.",
  "scripts": {
    "dev": "next dev -p 3240",
    "build": "next build",
    "start": "next start -p 3240",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.5.25",
    "grc-program": "file:..",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "20.14.9",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "typescript": "5.5.3"
  }
}
